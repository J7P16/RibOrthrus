Goals for Today:
----------------

- Need to setup a sequence extractor from gencode.v48 reference (GenomeKit
    doesn't provide v48 in their documentation)
- Need to work on padding mechanism for sequence below/above 13,000 nucleotides
- Develop new model to take in embeddings and predict ribosome
    occupancy (must match exact input sequence length)
- Write embedding update email to grad advisor
- Finish iXnos training/tuning on new RiboBase data
- Keep track of orthrus environment setup and dependencies by test creating
    a new environment (for reproducibility)

- Must create a fresh and working Orthrus env in my vista machine and
    keep track of my dependencies/commands (failed yesterday -> will ask my advisor for help) (Done!)

- Take a deep dive into mutinomial loss function for project! 
	- After installing jaxtyping dependency, multimonial loss function looks healthy on various test cases.
	- Need to see if I can connect python scripts by importing loss into main pipeline script.(Done!)

- Take a look at Ribo file repository and understand RiboPy interface function calls and which ones I need
	- Need a function to grab Coverage Data for ribosome counts from ribo file and build small pipeline to convert to 		a y_true tensor (if I even get there today) (WIP).

- DETAILED PLAN TO FINISH DATA PROCESSING
	
	1. create data directory and sim link just ONE ribo file for now (Done!)
	2. use RiboPy to retrieve important features from the test ribo file (see if I can print it) (Done!)
	3. prepare arguments to retrieve coverage data (Done!)
	4. set y_true to features in the coverage data (Done!)

- Reference name for my lab's current ribo files: appris-v1 -> GenomeKit's GENCODE-v41

- DETAILED PLAN TO CONNECT DATA PROCESSING AND EMBEDDING GENERATION SCRIPTS

	1. Change the gencode verison being used to initialize my genome object
	2. Create dataset using data processing script
	3. Iterate through dataset and grab intervals by our data's transcript name
	4. MAYBE - create my own embedding directory and store generate embeddings -> limit generate_embeddings.py to just 		generation code -> creating own model.py -> train.py
	5. WIP might need to think of more steps

- Deeply look at RiboPy interface source code and find out which specific function calls are needed for cds boundary signals (Done!)

- Create and seperate three diff Orthrus x Dilated Convolution training sessions (ribo seq, rna seq, ribo/rna seq) and take note across the three different pcc metrics.
	- NOTE: MUST USE LOG1P NORMALIZATION: log(1 + x) ACROSS RNA VALUES OR CALCULATIONS BREAK 

- Refactoring RibOrthrus model pipeline to different python files such that:
RibOrthrus/
|- models/
 |- orthrus-6-track/
|- data/
 |- A2780.ribo
 |- ...
 |- U-343.ribo
|- embeddings/
|- scripts/
 |- prepare_data.py
 |- generate_embeddings.py
 |- model.py
 |- loss.py
 |- train.py

NOTES ON PIPELINE AND MODEL ARCHITECTURE:
=========================================
- Adding just one more linear layer and GELU activation to head seems to slightly improve PCC performance (0.25 -> 0.30)
	- Bonus: loss severely decreases even without data normalization
- Implemented a custom ConvRiboHead to process embeddings -> hopefully makes much better predictions than Linear model
	- Greatly improved PCC performance (0.30 -> 0.33) (keep around but not optimal)
- DenseRateHead with Dilated Convolution Blocks (BEST PERFORMANCE of ~0.44) 
	- Group Norm
	- Orthrus expects entire, mature transcripts -> Head requires less blocks
	- Transposing input tensor between Length and Cell dimensions before head and transposing back after is key

- Implement Codon resolution setting

- Implement CLI arguments for my training script to set diff things using argparse python library
	- Hyperparameters
	- Num workers
	- Resolution
	- Ribo Seq vs RNA Seq vs Both prediction
	- Fixed Sequence Length

VISTA ARM64 ARCHITECTURE SETUP:
===============================
mamba env create -f env.yml (removed original pytorch-cuda=12.1 and cuda-toolkit=12.1 lines b/c of vista architecture)
	- edited pytorch to 2.6 version for pytorch-gpu and cuda-toolkit compatibility on vista

conda activate orthrus

export CC=gcc
export CXX=g++
export CUDAHOSTCXX=g++

export CUDA_HOME=$CONDA_PREFIX
export CPATH=$CONDA_PREFIX/targets/sbsa-linux/include:$CONDA_PREFIX/include:$CPATH
export C_INCLUDE_PATH=$CONDA_PREFIX/targets/sbsa-linux/include:$C_INCLUDE_PATH
export CPLUS_INCLUDE_PATH=$CONDA_PREFIX/targets/sbsa-linux/include:$CPLUS_INCLUDE_PATH export LIBRARY_PATH=$CONDA_PREFIX/targets/sbsa-linux/lib:SCONDA_PREFIX/lib:SLIBRARY_PATH
export LD_LIBRARY_PATH=$CONDA_PREFIX/targets/sbsa-linux/lib:SCONDA_PREFIX/lib:SLD_LIBRARY_PATH

export MAX_JOBS=1 (optional but might be useful)

pip install git+https://github.com/Dao-AILab/causal-conv1d.git@v1.2.0.post2

pip install --no-build-isolation git+https://github.com/state-spaces/mamba.git@v1.2.0.post1

[CLONE THE ORIGINAL Orthrus REPOSITORY AND cd TO IT]
pip install -e . --no-build-isolation 

pip install jaxtyping

pip install ribopy (do receive one tiny dependency mismatch but seems to not affect anything about the data pipeline
	so far yay!)

pip install lightning
